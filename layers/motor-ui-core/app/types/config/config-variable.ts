import type { FormFieldsFromMetaOptions } from '../../composables/formFieldUtils'

export const configVariableFormConfig: FormFieldsFromMetaOptions = {
  groups: {
    basic: ['package', 'group', 'name', 'value', 'is_invisible']
  }
}
