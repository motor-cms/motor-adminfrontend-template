---
'@motor-cms/ui-core': patch
---

fix(search): the Cmd+K search palette's copy-link action now also copies the file's public storage/CDN URL instead of the VPN-only backend /download route — the third and last copy path, after the gallery/grid and the full search-results page
