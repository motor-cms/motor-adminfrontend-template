---
'@motor-cms/ui-media': patch
---

fix(media): the lightbox (enlarged image) now receives the public storage/CDN URL as well, so its copy-link button no longer shares the VPN-only backend /download route — completes the copy-url fix for the gallery and grid
